{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "rimraf docs && observable build && touch docs/.nojekyll",
    "dev": "observable preview",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.10.1",
    "@observablehq/plot": "^0.6.16",
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
